#include <stdio.h>

int main () {
    int x = 10;
    int y = 11;
    int z = x + y;
    printf ("the sum of %d + %d = %d", x,y,z ); //words or sentence can also be written in the printf function the %d , %c means the place holder are the important only and words are kept stored in double strokes.

    return 0;
}